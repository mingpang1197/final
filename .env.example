# Upstage API (https://console.upstage.ai/)
UPSTAGE_API_KEY=

# Mock mode when API key is empty (uses sample text/responses)
# Vercel 배포 시 반드시 false 로 설정
MOCK_UPSTAGE=true

# Server
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8001

# PDF 변환 (Vercel 등 서버 환경 — Word/LibreOffice 없을 때)
# https://www.convertapi.com/a/signup (무료 250건/월)
CONVERTAPI_SECRET=
