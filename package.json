{
  "name": "easyread",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend && node scripts/copy-images.mjs"
  }
}
